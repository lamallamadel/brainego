# Deployment Script Dependencies
#
# Install with: pip install -r requirements-deploy.txt
#

# YAML parsing for Helm charts and Kubernetes manifests
pyyaml>=6.0.1

# Kubernetes Python client for programmatic K8s API access
kubernetes>=28.1.0

# HTTP requests for smoke tests and API validation
requests>=2.31.0

# AWS Route53 DNS management (for multi-region failover)
boto3>=1.26.0

# Google Cloud DNS management (for multi-region failover)
google-cloud-dns>=0.34.0
