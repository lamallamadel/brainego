## Summary
- Describe what this PR changes and why.

## Validation
- List the exact commands you ran and their outcomes.

## No-Noise Checklist
- [ ] No test in this PR depends on Internet access (no external HTTP/DNS lookups).
- [ ] Validation did not use online package installation (`pip install` without `--no-index --find-links=vendor/wheels`).
- [ ] Integration tests were **not** run from Codex environments (integration runs only in CI Docker/services).

## Additional Notes
- Mention any `# Needs:` declarations introduced by this PR.
- Link related issue(s): AFR-49
