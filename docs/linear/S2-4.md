# S2-4 — SupportCheck (judge) anti “réponse non supportée”

## Scope
- Extended support-check downgrade instrumentation to explicit unsupported-answer metrics.
- Added `unsupported_answer_events` counter and `unsupported_answer_rate` in metrics payload.
- On support-check failures, responses are downgraded to `missing_context` and unsupported metric increments.

## Implementation notes
- Support-check runs in all RAG generation paths.
- Existing false-citation telemetry is preserved and now complemented with unsupported-answer telemetry.

## Validation
- Added static wiring tests confirming unsupported-answer metric exposure and recording hooks.
