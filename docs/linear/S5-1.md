# S5-1 — Dataset builder (learning_events → versioned JSONL)

## Scope
- Added `learning_dataset_builder.py` to export distillation samples from `learning_events`.
- Enforced supported-only selection (`supported=true`) and automatic rejection for secret leaks.
- Output is versioned JSONL (`learning_distill_v<timestamp>.jsonl`).

## Validation
- Added unit tests for supported filtering, secret-leak rejection, and missing-field rejection.
