# S4-1 — Deterministic overlay priority selection

## Scope
- Updated `overlay_router.py` so overlay matching is ordered by descending `priority`.
- Preserved intent + query substring matching semantics.

## Validation
- Added unit coverage asserting highest-priority matching rule wins.
