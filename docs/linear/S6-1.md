# S6-1 — Prod smoke + rollback gate (VM)

## Scope
- Validated VM deploy flow enforces post-deploy smoke tests and rollback path on failure.
- Confirmed skip-smoke path is auditable through `skip-smoke.log`.

## Validation
- Added static wiring tests for smoke gate + rollback + skip-smoke audit hooks in `scripts/deploy/deploy_vm.sh`.
