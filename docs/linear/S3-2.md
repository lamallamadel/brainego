# S3-2 — RetrievalRecipes store (query rewrite / filters / topK params)

## Scope
- Added `retrieval_recipes_store.py` with workspace-scoped versioned recipe storage and rollback.
- Added recipe application helper to rewrite query and override retrieval parameters (`top_k`, optional filters).
- Integrated recipe application into `/v1/rag/query` and `/v1/rag/query/graph-enriched` before retrieval.
- Exposed recipe execution metadata in retrieval stats:
  - `recipe_applied`
  - `rewritten_query`

## Validation
- Added unit tests for store versioning, rollback, and recipe application behavior.
- Added static API wiring tests for recipe integration.
