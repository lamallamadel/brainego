# S3-4 — OverlayRouter in inference path (before LLM)

## Scope
- Added `overlay_router.py` with rule matching by intent + query substring.
- Integrated overlay router in `/v1/chat/completions` before model generation.
- If a matching overlay rule is found, API returns deterministic overlay response without LLM call.
- Added routing metadata marker: `overlay_applied=true`.

## Validation
- Added unit tests for overlay router matching behavior.
- Added static wiring tests confirming pre-LLM overlay integration.
