# S5-3 — LoRA trainer batch + eval + rollback adapters

## Scope
- Added weekly LoRA orchestration script: `scripts/learning/lora_weekly_batch.sh`.
- Script triggers `POST /train/jsonl`, runs canary gate with timeout, and executes rollback command on failure.
- Parameters are environment-driven (`DATASET_PATH`, `ADAPTER_VERSION`, `CANARY_TIMEOUT_SEC`, `ROLLBACK_CMD`).

## Validation
- Added static wiring tests asserting train trigger, canary timeout gate, and rollback hook are present.
