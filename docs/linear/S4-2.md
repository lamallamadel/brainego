# S4-2 — Overlay trace metadata in `/v1/chat/completions`

## Scope
- Added `pick_overlay_match` usage in API overlay short-circuit path.
- Extended routing metadata with `overlay_rule_id` and `overlay_rule_priority`.

## Validation
- Updated static wiring tests to assert new metadata keys are emitted.
