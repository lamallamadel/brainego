# S6-3 — Alertmanager Slack + pilot-critical alerts

## Scope
- Hardened Slack alert templates in `configs/alertmanager/alertmanager.yml`.
- Added explicit `Runbook` field (`.CommonAnnotations.runbook_url`) in default and critical alert notifications.
- Preserved critical escalation (`@channel`) and Slack webhook wiring.

## Validation
- Added static tests validating Slack webhook and runbook URL rendering in alert notifications.
