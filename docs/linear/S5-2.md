# S5-2 — QualityFilter (supported + feedback + policy)

## Scope
- Added `apply_quality_filter` in `learning_dataset_builder.py`.
- Rejects samples with no evidence, low feedback score, or policy rejection.
- Exposes `precision_estimate` and `meets_precision_target` gate (>=95%).

## Validation
- Added unit tests for rejection reasons and precision gate behavior.
