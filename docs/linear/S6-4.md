# S6-4 — Log hygiene + secret marker verification

## Scope
- Added `scripts/observability/verify_log_hygiene.py` to scan logs for unredacted secrets.
- Script reports `secret_hits` and `redaction_marker_hits`, and exits non-zero when any secret leak is detected.
- Supports CI/ops gating requirement: `0 secret in logs`.

## Validation
- Added unit tests for secret detection and redaction marker accounting.
