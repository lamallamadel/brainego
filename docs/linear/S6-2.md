# S6-2 — k6 CI merge suite + nightly

## Scope
- Extended `.github/workflows/load-test.yml` to trigger on `pull_request` and nightly `schedule`.
- Added dedicated `nightly-load-test` job executing `scripts/load_test/run_k6_nightly_suite.sh`.
- Kept merge/push path using `scripts/load_test/run_k6_suite.sh`.
- Added fail-fast DNS/health preflight controls in both scripts to avoid long `no such host` loops (`K6_FORCE_RUN`, `K6_ABORT_ON_HEALTHCHECK_FAILURE`).

## Validation
- Added static workflow tests for PR + schedule triggers, force-run wiring, and nightly/merge script wiring.
