# S1-5 — Hard partition workspace filters everywhere

## Scope
- Enforced workspace partition checks on retrieval paths with explicit cross-workspace assertion in `rag_service.py`.
- Added structured audit trace logging carrying `workspace_id` on RAG request entry points.

## Implementation notes
- New structured trace helper in `api_server.py`:
  - `emit_workspace_audit_trace(endpoint, workspace_id, event)`
- Trace is emitted for:
  - `/v1/chat/completions`
  - `/v1/rag/query`
  - `/v1/rag/query/graph-enriched`
- Retrieval still enforces `workspace_id=resolved_workspace_id` and raises on leakage.

## Validation
- Added static wiring tests for audit trace emission and workspace partition assertions.
