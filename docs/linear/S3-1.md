# S3-1 — OverlayRules store + versioning + rollback

## Scope
- Added `overlay_rules_store.py` to persist overlay rules with:
  - versioned snapshots
  - active version pointer
  - per-version enable/disable
  - rollback to any prior version
- Active rule retrieval is workspace-scoped.

## Validation
- Added unit tests covering version creation, enable/disable behavior, active-rule lookup, and rollback.
